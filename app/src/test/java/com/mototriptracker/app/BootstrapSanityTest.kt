package com.mototriptracker.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * FND-001 acceptance requires that "a basic local test command passes".
 * This is that placeholder; real domain tests start in FND-004/TST-001.
 */
class BootstrapSanityTest {

    @Test
    fun jvmUnitTestInfrastructureWorks() {
        assertEquals(4, 2 + 2)
    }
}
