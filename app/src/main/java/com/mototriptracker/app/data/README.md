# data

Repositories and mappers per `docs/03-architecture/system-architecture.md`
(F0.8) §4/§13: exposes the source of truth to `domain`/UI, abstracting Room
and DataStore. `repository/` and `mapper/` are created by `FND-003` onward.
