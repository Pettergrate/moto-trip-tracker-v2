# feature

Compose screens and ViewModels, one subpackage per F0.9 destination
(`home/`, `active/`, `history/`, `tripdetail/`, `favorites/`, `routes/`,
`settings/`). Created starting with `UI-001` (Home/Active Trip) and
`HIS-001` (History/Trip Detail).
